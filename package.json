{
  "name": "ico-token",
  "version": "0.1.0",
  "description": "$ICO token",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "truffle test",
    "migrate": "truffle migrate --reset",
    "server": "cp supply-config.json src/config.json && lite-server",
    "server:ico": "cp src/ico/* src/ && yarn run server",
    "console": "truffle console"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "lite-server": "2.6.1",
    "chai": "^4.3.4",
    "mocha": "^9.0.2"
  }
}
